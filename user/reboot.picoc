// dependencies: ../library/unistd/libunistd.reti_blocks

#include "../common/syscall.header"
#include "../library/unistd/unistd.header"

int main(void) {
    invoke_syscall(SYSCALL_REBOOT, 0);
    return 0;
}
