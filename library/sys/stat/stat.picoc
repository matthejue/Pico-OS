#include "stat.header"
#include "../../unistd/unistd.header"
#include "../../../common/syscall.header"

int mkdir(char *path) {
    if (path == NULL) {
        return -1;
    }
    return invoke_syscall(SYSCALL_MKDIR, (int)path);
}
