// dependencies: ../../lib/process/libprocess.picoc

#include "../../lib/process/process.header"

int main(void) {
    int pid = load("sys_tests/args/args.bin");

    run(pid, "42 beta gamma");
    wait(pid);
    return 0;
}
