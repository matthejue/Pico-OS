// dependencies: ../../lib/stdio/libstdio.picoc ../../lib/schedule/libschedule.picoc

#include "../../lib/stdio/stdio.header"
#include "../../lib/schedule/schedule.header"

int main(int argc, char **argv) {
    int count = 0;

    while (count < 4) {
        printf("%s\n", argv[1]);
        yield();
        count = count + 1;
    }

    return 0;
}
