// in:8 6
// expected:12 42

#include "../lib/malloc/malloc.h"
#include "../lib/malloc/malloc.picoc"

int main() {
    init_heap();

    int *a = (int *)malloc(input());
    a[0] = 3;
    a[7] = 4;

    int *b = (int *)malloc(input());
    b[0] = 21;
    b[5] = 2;

    print(a[0] * a[7]);
    print(b[0] * b[5]);

    return 0;
}
