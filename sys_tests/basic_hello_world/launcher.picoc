// dependencies: ../../lib/unistd/libunistd.picoc

#include "../../lib/unistd/unistd.header"
#include "../../common/stddef.header"

int main(void) {
    int pid = load("sys_tests/basic_hello_world/hello_world.bin");

    run(pid, NULL, NULL);
    wait(pid);
    return 0;
}
