#pragma once

struct Process;

struct Process *dispatcher_tick(
    int saved_sp,
    int saved_ds,
    int saved_cs,
    int saved_baf,
    int saved_in2,
    int saved_in1,
    int saved_acc
);
void dispatcher_save_current(
    int saved_sp,
    int saved_ds,
    int saved_cs,
    int saved_baf,
    int saved_in2,
    int saved_in1,
    int saved_acc
);
void dispatcher_start_next_process(void);
