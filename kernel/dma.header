#pragma once

#include "../common/dma.header"

void initialize_dma(void);
bool start_dma_uart_receive(
    int destination,
    int word_count,
    int *caller_context
);
void handle_dma_interrupt(void);
