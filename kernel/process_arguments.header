#pragma once

#include "process.header"
#include "../common/syscall.header"

int process_store_arguments(struct Process *process, char *arguments);
int process_mark_ready_with_arguments(struct RunProcessRequest *request);
